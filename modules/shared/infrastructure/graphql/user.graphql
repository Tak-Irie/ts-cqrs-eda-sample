type Query {
  user(id: ID!): User!
}

type Mutation {
  registerUser(userName: String!, email: String!, password: String!): User!
}

type User {
  id: ID!
  userName: String!
  email: String!
  profile: Profile
}
